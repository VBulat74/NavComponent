<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NavComponent" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        
        <item name="materialButtonStyle">@style/GlobalButtonStyle</item>
    </style>

    <style name="Theme.NavComponent" parent="Base.Theme.NavComponent" />

    <style name="GlobalButtonStyle" parent="Widget.Material3.Button">
        <item name="android:minWidth">200dp</item>
    </style>
</resources>